<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chapterandcontentfragment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_all_in_one"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_all_in_one_round"
        android:supportsRtl="true"
        android:logo="@drawable/all_in_one_title"
        android:theme="@style/AppTheme.ActionBar.Transparent">
        <activity android:name=".CourseActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChapterAndContentActivity"/>
        <activity android:name=".BoardActivity"/>
    </application>





</manifest>